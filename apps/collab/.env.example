# The collab server reads DATABASE_URL and CLERK_SECRET_KEY from apps/api/.env
# automatically (single source for local dev). You only need this file to
# override the WebSocket port.
COLLAB_PORT=1234
