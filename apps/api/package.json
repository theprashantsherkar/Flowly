{
  "name": "@flowly/api",
  "version": "0.1.0",
  "private": true,
  "description": "Flowly REST API + RBAC (NestJS + Prisma). Implemented in Phase 1.",
  "scripts": {
    "start:dev": "echo \"@flowly/api is scaffolded in Phase 1\" && exit 0"
  }
}
