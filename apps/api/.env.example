# Postgres (matches docker-compose.yml)
DATABASE_URL="postgresql://flowly:flowly@localhost:5432/flowly?schema=public"

# Clerk — create a free app at https://dashboard.clerk.com and paste the keys.
# The API only needs the secret key to verify session tokens.
CLERK_SECRET_KEY="sk_test_xxx"

# Server
PORT=4000
WEB_ORIGIN="http://localhost:3000"
