# Clerk publishable key — from https://dashboard.clerk.com (safe to expose to the browser)
VITE_CLERK_PUBLISHABLE_KEY="pk_test_xxx"

# Base URL for the API. In dev, leave as /api (Vite proxies it to the API server).
VITE_API_URL="/api"
